# Unraid Production Environment Configuration
# Uses Docker container mount when running on Unraid server
# Accesses same Unraid media as local but via proper container mounting

# Media Mount Configuration (Docker container mount)
MEDIA_SOURCE=/mnt/user/media
MEDIA_TARGET=/app/media
MEDIA_MODE=ro

# Environment Type
ENV_TYPE=unraid
MEDIA_DESCRIPTION=Unraid container mount (18,509+ files via /mnt/user/media)
UNRAID_ACCESS_METHOD=container_mount
UNRAID_MEDIA_PATH=/mnt/user/media

# Database Configuration
DB_NAME=watch1_dev
DB_USER=watch1_user
DB_PASSWORD=watch1_dev_password

# Production Settings
FLASK_ENV=production
FLASK_DEBUG=false
NODE_ENV=production

# API Configuration
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
JWT_SECRET_KEY=dev-jwt-secret-key-change-in-production

# Logging
LOG_LEVEL=INFO
